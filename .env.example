# JaneDeck Environment Variables
# Copy this file to .env and fill in the values.

# Admin password for host authentication (required)
# This password gates game creation — only authenticated hosts can create games.
JANEDECK_ADMIN_PASSWORD=change-me-to-a-strong-password
